[package]
name = "weldspeak-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Wire protocol shared between the WeldSpeak desktop client and the Cloudflare Worker"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
