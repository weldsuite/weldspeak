# Clerk publishable key (pk_test_... or pk_live_...), from the Clerk dashboard.
# This one is public and safe in the browser; the secret key belongs only in the
# Worker, set with `wrangler secret put CLERK_SECRET_KEY`.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_replace_me
